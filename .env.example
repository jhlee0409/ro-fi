# 🤖 Claude Code Actions 전용 설정
# 이 프로젝트는 100% Claude Code GitHub Actions을 사용합니다
# 직접 API 호출은 사용하지 않습니다

# GitHub Secrets에 설정해야 하는 값들:
# ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxx (Claude Code Actions용)
# VERCEL_TOKEN=xxxxx (배포용)
# VERCEL_ORG_ID=xxxxx 
# VERCEL_PROJECT_ID=xxxxx

# 로컬에서는 API를 사용하지 않으므로 .env.local 파일이 필요하지 않습니다
# 모든 AI 생성은 GitHub Actions에서 Claude Code Actions으로만 처리됩니다